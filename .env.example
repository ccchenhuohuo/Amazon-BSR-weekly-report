# Copy to a local .env file or export these values in your shell.
# Never commit real credentials.

# Sorftime
SORFTIME_API_KEY=
SORFTIME_DOMAIN=1
SORFTIME_TIMEOUT=120

# Doris
DORIS_HOST=doris.example.invalid
DORIS_MYSQL_PORT=30930
DORIS_STREAM_LOAD_HOST=doris-stream-load.example.invalid
DORIS_STREAM_LOAD_PORT=33060
DORIS_STREAM_LOAD_FALLBACK_HOST=
DORIS_STREAM_LOAD_FALLBACK_PORT=
DORIS_USER=
DORIS_PASSWORD=
DORIS_DATABASE=
DORIS_TABLE=

# Weekly report output
# Defaults to ./reports when unset.
SORFTIME_REPORT_OUTPUT_DIR=

# Feishu/Lark Base
FEISHU_TEMPLATE_BASE_TOKEN=
FEISHU_WEB_ORIGIN=https://ulanzichina.feishu.cn
# Use an absolute path in cron environments.
LARK_CLI_BIN=/usr/local/bin/lark-cli
LARK_CLI_TIMEOUT_SECONDS=240
# Optional when lark-cli credentials live outside its default data dir.
LARKSUITE_CLI_DATA_DIR=

# Feishu/Lark completion notification
# Set one recipient. FEISHU_NOTIFY_CHAT_ID takes precedence when both are set.
FEISHU_NOTIFY_USER_ID=
FEISHU_NOTIFY_CHAT_ID=
FEISHU_NOTIFY_AS=bot
# Set to 1 in production so a missing/failed notification makes the workflow fail.
FEISHU_REQUIRE_NOTIFY=0

# BSR backfill
MAX_WORKERS=4
START_DATE=2026-01-01
TARGET_WEEKDAY=wednesday
